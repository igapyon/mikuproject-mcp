{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://igapyon.github.io/mikuproject-mcp/contract/tools/mikuproject_ai_export_phase_detail.input.schema.json",
  "title": "mikuproject_ai_export_phase_detail input",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "phaseUid"
  ],
  "properties": {
    "workbookPath": {
      "type": "string",
      "minLength": 1,
      "description": "Path to a mikuproject workbook JSON file passed to the upstream CLI as --in."
    },
    "phaseUid": {
      "type": "string",
      "minLength": 1,
      "description": "Phase task UID passed to the upstream CLI as --phase-uid."
    },
    "mode": {
      "type": "string",
      "enum": [
        "scoped",
        "full"
      ],
      "description": "Optional phase-detail mode passed to the upstream CLI as --mode."
    },
    "rootTaskUid": {
      "type": "string",
      "minLength": 1,
      "description": "Optional scoped root task UID passed to the upstream CLI as --root-task-uid."
    },
    "maxDepth": {
      "type": "integer",
      "minimum": 0,
      "description": "Optional non-negative scoped depth passed to the upstream CLI as --max-depth."
    },
    "outputPath": {
      "type": "string",
      "minLength": 1,
      "description": "Optional path for the generated phase_detail_view projection."
    },
    "workbookContent": {
      "type": "string",
      "minLength": 1,
      "description": "Inline mikuproject workbook JSON content passed on stdin."
    },
    "outputMode": {
      "type": "string",
      "enum": [
        "path",
        "content"
      ],
      "description": "Output mode. Omit or use path to write an artifact path; use content to return text content directly."
    }
  },
  "allOf": [
    {
      "oneOf": [
        {
          "required": [
            "workbookPath"
          ],
          "not": {
            "required": [
              "workbookContent"
            ]
          }
        },
        {
          "required": [
            "workbookContent"
          ],
          "not": {
            "required": [
              "workbookPath"
            ]
          }
        }
      ]
    },
    {
      "not": {
        "required": [
          "outputPath",
          "outputMode"
        ],
        "properties": {
          "outputMode": {
            "const": "content"
          }
        }
      }
    }
  ]
}
